## 1. Documentation

- [x] 1.1 Create `docs/google-auth-setup.md` with instructions for Google Cloud Console.

## 2. Environment Configuration

- [x] 2.1 Add `GOOGLE_CLIENT_ID` and `GOOGLE_CLIENT_SECRET` to `packages/env/src/server.ts`.
- [x] 2.2 Update `.env.example` (if it exists) or document the need for these variables.

## 3. Backend Implementation

- [x] 3.1 Update `packages/auth/src/index.ts` to enable `socialProviders` with Google.

## 4. Frontend Implementation

- [x] 4.1 Update `apps/web/src/components/sign-in-form.tsx` to include Google login button.
- [x] 4.2 Update `apps/web/src/components/sign-up-form.tsx` to include Google login button.
